<%= render "compartilhado/breadcrumb", pag: "Todos Gêneros" %>

<h1>Todos os Generos</h1>

<% if signed_in? %>
<p>
	<span>Não achou o genero que procurava?</span> 
	<%= link_to 'Crie um novo genero', new_genero_path %>
</p>
<% end %>

<table class="table table-hover">
		<tr class="info">                      
		<th><%= sortable "nome" %></th>
		<th><%= sortable "descricao" %></th>
		<% if is_admin? %>
			<th>Editar</th>
			<th>Excluir</th>
		<% end %>
	</tr>
  <%= render @generos %>
</table>

<br>
