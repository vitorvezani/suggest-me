<li>
  <%= link_to usuario.username, usuario %>
  <% if is_admin? && current_user != usuario %>
      | <%= link_to "excluir", usuario, method: :delete,
                              data: { confirm: "Você tem certeza?" } %>
	<% end %>
</li>